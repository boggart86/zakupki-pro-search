[project]
name = "zakupki-pro-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chamomile", email = "snakecode86@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
zakupki-pro-search = "zakupki_pro_search:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
